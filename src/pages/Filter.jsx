import React from "react";
import { Form } from "react-router-dom";

export function Filter(){
    return (
        <Form className="filter-form">
            <h2 className="form-title">Filter values</h2>
        </Form>
    )
}